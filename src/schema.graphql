type Query {
    read(id: ID!): Comics!
}

type Mutation {
    create(name: String!, author: String!): Comics!
    update(id: ID!, name: String!, author: String!): Comics!
    delete(id: ID!): Int!
}

type Comics {
    id: ID!
    name: String!
    author: String!
}